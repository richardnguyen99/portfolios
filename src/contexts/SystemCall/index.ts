import SystemCallContext from "./Context";
import SystemCallProvider from "./Provider";
import useSystemCall from "./useSystemCall";

export default {
  Context: SystemCallContext,
  Provider: SystemCallProvider,
  useSystemCall,
};
