import ClipboardContext from "./Context";
import ClipboardProvider from "./Provider";
import useClipboard from "./hook";

export default {
  Context: ClipboardContext,
  Provider: ClipboardProvider,
  useClipboard,
};
